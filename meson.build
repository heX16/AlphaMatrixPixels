project('AlphaMatrixPixels', 'cpp',
  version : '1.0.0',
  default_options : [
    'cpp_std=c++17',
    'warning_level=2',
    'default_library=static'
  ]
)

# Header-only library - no build needed, just include directories
amp_inc = include_directories('.')

# Subdirectories
subdir('GUI_Test')

